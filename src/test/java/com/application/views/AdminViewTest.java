package com.application.views;

import com.application.views.admin.AdminView;
import org.junit.jupiter.api.Test;

class AdminViewTest {

    @Test
    void beforeEnter() {
        var adminView = new AdminView();
        //assertFalse(adminView.beforeEnter(new BeforeEnterEvent()););
    }
}